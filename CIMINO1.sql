-- use world;
-- SELECCION. UNICIDAD. ALIAS. COLUMNAS CALCULADAS
-- 1
-- select `Name`, `Continent` , `Population` from country;

-- 2
-- Select distinct `Language` from countrylanguage;

-- 3
-- Select `Name` as `Nombre`, `Population` as `Poblacion` from city;

-- 4
-- select `Name`, `GNP` as `PBN`, `GNPOld` as `PBNA`,
-- `GNP`-`GNPOld` as `Diferencia` from country;

-- 5
-- select `Name`, `Population`, `SurfaceArea`,
-- `Population`/`SurfaceArea` as `Densidad` from country;

